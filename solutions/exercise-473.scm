; There is no guarantee the input stream will be finite.  Without an explicit
; delay, flatten-stream could recurse indefinitely without returning control
; to the caller to operate on the resultant stream.
