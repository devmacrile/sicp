; Alyssa's version will create the sequence of executions at
; run-time, whereas the version in the text builds out the
; whole expression at analysis time.
; 
; For one expression, it should be the same (?), since there's 
; is only one call in both; for two, Alyssa's will do more 
; work during each execution as it executes each expression
; sequentially.