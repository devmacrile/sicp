; Sounds reasonable to me w.r.t. computation.  In the top form (i.e. R1*R2 / (R1 + R2)), there
; are two expressions which involve the error/tolerance terms associated 
; with each resistance interval.
;
; But, still seems tough to call one ~better~ simply based on that reasoning (i.e. resulting
; tightness of interval bounds).  Seems to have more to do with specifics of interval arithmetic.